@model ECOMMERCE_TRESB.ModelViews.CategoriaView
@{
    ViewBag.Title = "Crear";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}
<span id="VerificacionCrearCategoriasTag"></span>
<h2>Agregar Categoria</h2>
<hr />
@if (ViewBag.ExisteCategoria != null)
{
    <div class="col-md-12">
        <div class="alert alert-danger alert-dismissable">
            <button aria-hidden="true" data-dismiss="alert" class="close" type="button" id="CerrarAlertaClic"> × </button>
            @ViewBag.ExisteCategoria
        </div>
    </div>
}
<form action="@Url.Action("Crear","Categoria")" method="post">
    <div class="form-group">
        <label for="Nombre" class="control-label">Nombre: </label>
        <input type="text" name="Nombre" id="NombreClic" value="@Model.Nombre" class="form-control" maxlength="100" onkeypress="return validarLetras(event)" />
        @Html.ValidationMessage("Nombre")
    </div>
    <div class="text-center">
        <button class="btn btn-primary" id="AgregarCateogiraClic">Agregar</button>
    </div>
</form>
<hr />
<br />
<a href="@Url.Action("Listar","Categoria")" id="RegresarListarClic" class="btn btn-lg btn-warning pull-left">Regresar</a>

@section scripts{
    <script src="~/Scripts/Validaciones.js"></script>
}

